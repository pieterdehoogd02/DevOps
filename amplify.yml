version: 1
applications:
  - appRoot: frontend/planning_app
    frontend:
      phases:
        preBuild:
          commands:
            - cd frontend/planning_app
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: frontend/planning_app/build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/** 
